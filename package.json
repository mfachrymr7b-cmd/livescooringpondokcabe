{
  "name": "live-scoring-pondokcabe",
  "version": "1.0.0",
  "description": "Live scoring golf frontend with Convex backend",
  "main": "index.js",
  "scripts": {
    "dev": "npx concurrently \"npm run convex:dev\" \"npm run dev --prefix app\"",
    "dev:app": "npm run dev --prefix app",
    "dev:backend": "npx convex dev",
    "build": "npm run build --prefix app",
    "convex:dev": "npx convex dev",
    "convex:deploy": "npx convex deploy",
    "convex:codegen": "npx convex codegen",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "convex": "^1.38.0",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^25.7.0",
    "typescript": "^6.0.3"
  }
}
